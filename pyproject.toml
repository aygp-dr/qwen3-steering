[project]
name = "qwen3-steering"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.51.0",
    "torch",
    "datasets",
]

[dependency-groups]
dev = [
    "pytest",
    "hypothesis",
    "schemathesis",
    "httpx",
]
