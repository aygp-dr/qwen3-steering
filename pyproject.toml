[project]
name = "qwen3-steering"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.51.0",
    "torch",
    "datasets",
    "accelerate>=1.13.0",
]

[dependency-groups]
dev = [
    "pytest",
    "hypothesis",
    "schemathesis",
    "httpx",
]
